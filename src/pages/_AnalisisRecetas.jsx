// Archivo temporal de trabajo — su contenido fue movido a Analisis.js
// (el componente AnalisisRecetas vive ahora dentro de Analisis.js).
// No se importa desde ningún lado, así que no entra al bundle.
// Se deja como módulo válido vacío porque el entorno no permite borrarlo.
export {}
